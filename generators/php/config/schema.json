{
    "type": "object",
    "required": [
        "packageName",
        "invokerPackage",
        "srcBasePath",
        "gitUserId",
        "gitRepoId",
        "artifactUrl",
        "licenseName",
        "developerOrganization",
        "developerOrganizationUrl"
    ],
    "properties": {
        "packageName": {
            "type": "string",
            "pattern": "^[0-9A-Za-z\\\\]+$"
        },
        "invokerPackage": {
            "type": "string",
            "pattern": "^[0-9A-Za-z\\\\]+$"
        },
        "srcBasePath": {
            "type": "string",
            "pattern": "^[0-9A-Za-z/]+$"
        },
        "gitUserId": {
            "type": "string",
            "pattern": "^[0-9a-z_-]+$"
        },
        "gitRepoId": {
            "type": "string",
            "pattern": "^[0-9a-z_-]+$"
        },
        "artifactUrl": {
            "type": "string"
        },
        "licenseName": {
            "type": "string",
            "pattern": "^[0-9a-zA-Z_-]+$"
        },
        "developerOrganization": {
            "type": "string"
        },
        "developerOrganizationUrl": {
            "type": "string"
        }
    },
    "additionalProperties": false
}
